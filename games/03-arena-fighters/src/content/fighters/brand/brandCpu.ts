import type { CpuStyle } from '../cpuStyle';

/** Brand keeps to a middle distance, throws fireballs from afar, and is happy to go in. */
export const BRAND_CPU: CpuStyle = {
  preferredRange: 64,
  aggression: 0.55,
  jumpiness: 0.15,
  projectileLove: 0.45,
  throwLove: 0.3,
};

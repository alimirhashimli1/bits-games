import type { CpuStyle } from '../cpuStyle';

/** Kestrel lives in the air: he jumps in and dives in from a distance, and leaps out of corners. */
export const KESTREL_CPU: CpuStyle = {
  preferredRange: 52,
  aggression: 0.6,
  jumpiness: 0.25,
  projectileLove: 0,
  throwLove: 0.2,
};

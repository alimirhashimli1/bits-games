import type { CpuStyle } from '../cpuStyle';

/** Osal closes in for tackles, and patches himself up when he gets the chance. */
export const OSAL_CPU: CpuStyle = {
  preferredRange: 40,
  aggression: 0.55,
  jumpiness: 0.12,
  projectileLove: 0,
  throwLove: 0.45,
};

import type { CpuStyle } from '../cpuStyle';

/** Kanan sends shockwaves from a distance and walks through the rest with his fists. */
export const KANAN_CPU: CpuStyle = {
  preferredRange: 60,
  aggression: 0.6,
  jumpiness: 0.12,
  projectileLove: 0.35,
  throwLove: 0.3,
};

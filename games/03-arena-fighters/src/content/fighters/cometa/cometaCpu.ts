import type { CpuStyle } from '../cpuStyle';

/** Cometa walks in for the clutch and goes over the top of anyone who sits still. */
export const COMETA_CPU: CpuStyle = {
  preferredRange: 42,
  aggression: 0.6,
  jumpiness: 0.2,
  projectileLove: 0,
  throwLove: 0.35,
};

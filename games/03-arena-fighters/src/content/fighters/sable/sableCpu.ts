import type { CpuStyle } from '../cpuStyle';

/** Sable keeps her distance behind her orbs, and steps through anyone who settles into a rhythm. */
export const SABLE_CPU: CpuStyle = {
  preferredRange: 62,
  aggression: 0.5,
  jumpiness: 0.12,
  projectileLove: 0.45,
  throwLove: 0.25,
};

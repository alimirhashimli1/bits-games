import type { CpuStyle } from '../cpuStyle';

/** Nova keeps her distance: long kicks at the edge of her reach, and a slow wave to walk in behind. */
export const NOVA_CPU: CpuStyle = {
  preferredRange: 44,
  aggression: 0.55,
  jumpiness: 0.12,
  projectileLove: 0.25,
  throwLove: 0.15,
};

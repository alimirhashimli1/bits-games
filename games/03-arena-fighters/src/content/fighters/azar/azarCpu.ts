import type { CpuStyle } from '../cpuStyle';

/** Azar works from a distance with his darts and lunges in when the opponent comes close. */
export const AZAR_CPU: CpuStyle = {
  preferredRange: 72,
  aggression: 0.58,
  jumpiness: 0.12,
  projectileLove: 0.45,
  throwLove: 0.25,
};

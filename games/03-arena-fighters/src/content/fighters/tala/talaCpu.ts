import type { CpuStyle } from '../cpuStyle';

/** Tala likes to be close, where her quick kicks work, and cartwheels in rather than waiting. */
export const TALA_CPU: CpuStyle = {
  preferredRange: 48,
  aggression: 0.65,
  jumpiness: 0.2,
  projectileLove: 0,
  throwLove: 0.25,
};
